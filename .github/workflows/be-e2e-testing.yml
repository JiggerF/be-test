name: be-e2e-testing

on:
  pull_request:
  workflow_dispatch: {}

jobs:
  run-test:
    permissions:
      pull-requests: write
    uses: ./.github/workflows/test.yml
    with:
      generate-report: yes
      coverage-path: coverage